
SHORT TERM
* use jquery instead of mixing it with traditional js at random
* updateConnectedComponent has a bug with empty cells (and the code is probably buggy)
* layout different from square
* level with different sizes/scores
* first layer of wrapped levels
* second layer of wrapped levels
* overlay with explanation on start
* facebook thumbnail
* handle empty username

LONG TERM
* rewrite using coffeescript
* choose colors as to minimize change
* avoid double entries in the highscore list
* anonimous scores
* optimize for mobile
  - unique click
  - menu
  - adapt to direction
  - test